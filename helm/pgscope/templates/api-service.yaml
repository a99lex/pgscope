apiVersion: v1
kind: Service
metadata:
  name: pgscope-api
  labels:
    app: pgscope-api
spec:
  type: {{ .Values.api.service.type }}
  selector:
    app: pgscope-api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: 8000
      protocol: TCP
